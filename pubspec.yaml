name: flutter_ioc_container
description: A lightweight, flexible, and high-performance dependency injection and service location library for Flutter
version: 2.0.1-beta
repository: https://github.com/MelbourneDeveloper/flutter_ioc_container

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  ioc_container: ^2.0.1-beta

dev_dependencies:
  austerity: ^1.2.0
  example:
    path: example
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  
